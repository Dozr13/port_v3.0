// interface InputFieldProps {
//   id: string;
//   type: string;
//   style?: string;
//   onChange: () => void;
// }

// const InputField = ({ id, type, style, onChange }: InputFieldProps) => {
//   <TextField id={id} type={type} onChange={onChange} style={style} />;
// };

// export default InputField;
